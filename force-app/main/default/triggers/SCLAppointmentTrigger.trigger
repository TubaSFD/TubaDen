trigger SCLAppointmentTrigger on SCL_Appointment__c (after delete, after insert, after update, before delete, before insert, before update) {
    //SCLTriggerFactory.createHandler(SCL_Appointment__c.sObjectType);
}