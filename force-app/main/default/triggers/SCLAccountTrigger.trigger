trigger SCLAccountTrigger on Account (before insert,before update,after insert,after update) {    
    /*
    if (Trigger.isBefore || Trigger.isAfter) {
        if (Trigger.isInsert || Trigger.isUpdate) {
            SCLTriggerFactory.createHandler(Account.sObjectType);
        }
    } 
	*/
}