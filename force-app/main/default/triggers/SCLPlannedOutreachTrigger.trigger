trigger SCLPlannedOutreachTrigger on SCL_PlannedOutreach__c (after delete, after insert, after update, before delete, before insert, before update) {
    //SCLTriggerFactory.createHandler(SCL_PlannedOutreach__c.sObjectType);
}