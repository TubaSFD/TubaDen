<aura:component implements="flexipage:availableForAllPageTypes,force:LightningQuickActionWithoutHeader,force:hasRecordId" access="global" controller="SCL_CopyToClipboard">    
    <aura:html tag="style">
        .slds-modal {
        visibility: hidden;
        display: none;
        }
        .slds-backdrop{
        display: none;        
        }        
    </aura:html>
    <aura:handler name="init" value="{!this}" action="{!c.doInit}" />
    <aura:handler event="aura:doneRendering" action="{!c.doneRendering}"/>
</aura:component>